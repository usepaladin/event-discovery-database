discover:
  require-data-encryption: false
  server-instance-id: 48a5cc47-7bb5-4044-a922-a3424d9f774f
server:
  port: 8069
spring:
  application:
    name: event-discovery
  config:
    import:
      - classpath:/exclude.yml
      - classpath:/datasource.yml
      - classpath:/kafka.yml
      - classpath:/logging.yml
      - classpath:/debezium.yml

